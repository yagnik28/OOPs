#include<bits/stdc++.h>
using namespace std;

/*
1) In Inheritance, one object acquires all the properties 
   and behaviours of it's parent object automatically.
2) We can reuse, extend OR modify attributes 
   and behaviours which are defined in other class.
3) The class which inherents the properties called Derived Class,
   and whose property is inhereted is called Parent class. 
    
*/

int main()
{
    

    return 0;
}